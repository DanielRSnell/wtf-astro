---
import "../styles/global.css";
import SEOMeta from "../components/seo/seo-meta.astro";

export interface Props {
  title?: string;
  description?: string;
  noindex?: boolean;
}

const { 
  title = "WooThatsFast", 
  description = "Premium WordPress resources and expert reviews",
  noindex = false 
} = Astro.props;
---

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    
    <!-- SEO Meta Tags -->
    <SEOMeta 
      title={title}
      description={description}
      noindex={noindex}
    />
    
    <!-- Preconnect to external domains -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    
    <!-- Umami Analytics -->
    <script defer src="https://analytics.umbral.ai/script.js" data-website-id="8232a0dd-fbc4-4ad6-b2b3-715bed75a742"></script>
    
    <!-- Enhanced Analytics Tracking -->
    <script is:inline>
      // Simplified analytics for focused layout
      (function() {
        if (!window.WTFAnalytics) {
          window.WTFAnalytics = { currentPage: null };
        }

        function trackPageView() {
          const path = window.location.pathname;
          
          // Check if already tracked
          if (window.WTFAnalytics.currentPage === path) return;
          window.WTFAnalytics.currentPage = path;
          
          const metadata = {
            path: path,
            title: document.title,
            url: window.location.href,
            timestamp: new Date().toISOString()
          };
          
          if (window.umami) {
            window.umami.track('page-view', metadata);
          }
          
          localStorage.setItem('wtf-last-page', JSON.stringify(metadata));
        }

        // Only use astro:page-load event
        document.addEventListener('astro:page-load', trackPageView);
      })();
    </script>
  </head>
  <body class="min-h-screen overflow-x-hidden bg-background text-foreground antialiased">
    <main>
      <slot />
    </main>
  </body>
</html>